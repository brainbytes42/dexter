version = ''

dependencies {
	compile project(':DexterCore')

	compile 'io.reactivex.rxjava2:rxjavafx:2.2.+' // 2.11.x for Java 11!
	compile 'org.controlsfx:controlsfx:8.+' // v9 only for Java9!

	compile 'com.ibm.icu:icu4j:65.1' // i8n (e.g. ordinal numbers, ...)
}
